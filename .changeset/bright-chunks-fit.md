---
"clanka": patch
---

Enforce the configured chunk character limit for oversized individual lines in both AST and fallback chunking. Split long lines without discarding content, retaining their source line numbers and metadata.
